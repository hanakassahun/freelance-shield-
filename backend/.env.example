# Database URL for Sequelize ORM
# Use SQLite during development:
DATABASE_URL="sqlite:./dev.db"

# Or switch to PostgreSQL in production by changing the single DATABASE_URL string:
# DATABASE_URL="postgres://user:password@localhost:5432/freelance_shield"
